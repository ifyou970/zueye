＃zueye
＃关于端口扫描程序的介绍
socket建立
在利用Python内置模块socket进行连接时，主要是对错误的捕获上，在这里使用socket.connect_ex（地址）方法，该方法与socket.connect（地址）方法比例，主要是失败时的动作，后则是直接引发异常，前者是返回一个错误指示器，成功时该指示器为0connect_ex（）方法，所以可以利用变量的值是否为0来判断端口是否开通。
